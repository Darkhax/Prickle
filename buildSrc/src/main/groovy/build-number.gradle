project.version = "${project.version}.${System.getenv('BUILD_NUMBER') ? System.getenv('BUILD_NUMBER') : 0}".toString()
project.getSubprojects().each { proj -> proj.version = project.version }
project.logger.lifecycle("Appending build number to version. Version is now ${project.version}")